eventManager.addHandler( "f7", lambda key: saveScreenshot( scheduler.time() ) )
eventManager.addHandler( "f8", lambda key: scheduler.togglePause() )
eventManager.addHandler( "f9", lambda key: objectProperties.toggle() )
eventManager.addHandler( "f6", lambda key: make_video() )
eventManager.addHandler( "escape", exit )
